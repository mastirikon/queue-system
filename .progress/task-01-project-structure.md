# 📋 Задание #1: Структура проекта

**Дата выдачи:** 2025-12-16  
**Дата завершения:** 2025-12-16  
**Статус:** ✅ Выполнено  
**Фаза:** Setup & Project Structure

---

## 🎯 Цель
Создать правильную файловую структуру Go-проекта для микросервисной архитектуры согласно best practices.

---

## 📁 Структура для создания

```
queue-system/
├── cmd/
│   ├── api/              # Точка входа API сервиса
│   └── worker/           # Точка входа Worker сервиса
├── internal/             # Приватный код (не импортируется извне)
│   ├── config/          # Конфигурация приложений
│   ├── domain/          # Бизнес-модели и типы
│   ├── handler/         # HTTP handlers для API
│   ├── queue/           # Asynq client/server обёртки
│   └── task/            # Task handlers для Worker
├── pkg/                  # Публичный переиспользуемый код
│   └── logger/          # Общий логгер
├── configs/              # Конфигурационные файлы (.env примеры)
├── docker/               # Dockerfile'ы
├── .github/
│   └── workflows/       # CI/CD
└── scripts/              # Вспомогательные скрипты
```

---

## 📝 Детальные инструкции

### 1. Создай структуру каталогов

```bash
mkdir -p cmd/api cmd/worker \
  internal/config internal/domain internal/handler internal/queue internal/task \
  pkg/logger \
  configs docker .github/workflows scripts
```

### 2. Создай `.gitignore`

В корне проекта создай файл `.gitignore` со следующим содержимым:

```gitignore
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Dependency directories
vendor/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build output
/bin/
/dist/

# Logs
*.log
logs/

# Docker
*.pid
```

### 3. Проверь результат

```bash
find . -type d -not -path '*/\.git/*' -not -path '*/.cursor/*' -not -path '*/.progress/*' | sort
```

---

## ✅ Критерии выполнения

- [x] Созданы все каталоги из структуры
- [x] Создан файл `.gitignore` с правильным содержимым
- [x] Структура проверена командой find
- [x] Вывод команды показан ментору

---

## 📝 Результат проверки

**Оценка:** ✅ Отлично!

Все требования выполнены:
- Структура каталогов соответствует Go best practices
- .gitignore настроен корректно
- Команда проверки выполнена успешно

---

## 📚 Теория: Структура Go-проекта

### Зачем `cmd/`?
- Содержит точки входа (main.go) для разных бинарников
- Каждый подкаталог = отдельное приложение
- Минимум логики, только инициализация

### Зачем `internal/`?
- Приватный код, не экспортируется наружу (защита Go компилятора)
- Бизнес-логика приложения
- Нельзя импортировать из других проектов

### Зачем `pkg/`?
- Публичный переиспользуемый код
- Можно импортировать в других проектах
- Используй осторожно (лучше держать всё в internal)

---

## 🎓 Дополнительное чтение

- [Go Project Layout](https://github.com/golang-standards/project-layout)
- [Internal packages in Go](https://go.dev/doc/go1.4#internalpackages)

---

**Когда закончишь** — покажи мне вывод команды проверки структуры и содержимое `.gitignore`. Я проверю и дам следующее задание. 🎯

